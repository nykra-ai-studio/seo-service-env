# --- Web app ---
flask
gunicorn
flask-cors

# --- HTTP + parsing ---
requests
beautifulsoup4
lxml

# --- Data wrangling ---
pandas
numpy

# --- HTML templating (Mustache-style) ---
pystache

# --- Google APIs (for GSC OAuth + PageSpeed Insights etc.) ---
google-api-python-client
google-auth
google-auth-httplib2
google-auth-oauthlib

# --- Google Trends helper ---
pytrends

# --- AI / LLM clients ---
openai>=1.3.0
anthropic>=0.40.0          # For Claude API (if using)
google-generativeai>=0.5.0 # For Gemini Pro API (if using)

# --- Other useful helpers ---
python-dotenv   # for loading .env locally
tqdm            # for progress/debug loops
